/**
 * Utilities for rendering NFA/DFA data to the canvas.
 * @module node_draw
 */

import { Link } from "../elements/link";
import { StateNode } from "../elements/node";
import { links, nodes } from "../main/state";
import { to_symbol } from "./shared_utils";

export function distribute_nodes() {

}

function randomize () {
    
}